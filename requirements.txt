pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
requests>=2.31
pytest>=7.4

# Web app (local FastAPI server + static frontend, replaces Colab/ipywidgets)
fastapi>=0.110
uvicorn>=0.29

# Google Drive sync (optional -- only needed if you want bet_log.csv /
# learned_weights.json to persist to Drive across devices)
google-api-python-client>=2.120
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2

# Colab notebook (legacy; still works if you prefer it)
ipywidgets>=8.0
